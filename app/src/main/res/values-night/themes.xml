<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.Movies" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your dark theme here -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:progressBarStyle">@style/DarkProgressBar</item>
    </style>

    <style name="Theme.Movies" parent="Base.Theme.Movies" />

    <!-- Dark mode ProgressBar style -->
    <style name="DarkProgressBar" parent="Widget.AppCompat.ProgressBar">
        <item name="android:indeterminateTint">@color/white</item>
    </style>
</resources>
